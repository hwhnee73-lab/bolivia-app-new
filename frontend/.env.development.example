# Base URL the React dev server should use when calling the backend.
# Point to the Nginx proxy exposed by docker compose (http://localhost/api)
# so you don't need the backend port published locally.
REACT_APP_API_URL=http://localhost/api
